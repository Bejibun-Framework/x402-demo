# A Base Sepolia private key with testnet ETH (gas for the deposit tx) and
# testnet USDC (to fund the escrow channel). Never use a mainnet key here.
EVM_PRIVATE_KEY=0xYourTestnetPrivateKeyHere

RESOURCE_SERVER_URL=http://localhost:4021
